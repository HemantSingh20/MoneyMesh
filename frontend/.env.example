# URL of the backend API server. During local development without Docker, this defaults to http://localhost:5000.
# Inside Docker, docker-compose overrides this value to http://backend:5000.
VITE_BACKEND_URL=http://localhost:5000
